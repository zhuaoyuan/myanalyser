# 20260311 pybroker 回测多过滤器链

## 需求背景
- 支持同时使用多种过滤条件缩小回测基金池。
- 一类：CSV 白名单（如 filtered_fund_candidates.csv）。
- 另一类：算出来的条件（如 max_funds 截断）。
- 采用方式 C：从模块导入注册表；主流程不感知 filter-csv 等额外输入，只支持多过滤器链式过滤。

## 设计与选型
- **过滤器抽象**：`FundFilter` 协议，`filter(candidates: set[str]) -> set[str]`。
- **过滤器实现**：每个过滤器读取自身环境变量，实现定制逻辑（如 FilteredCandidatesCsvFilter 解析 filtered_fund_candidates.csv）。
- **注册表**：`backtest/filters/registry.py`，`FUND_BACKTEST_FILTERS=filtered_candidates,max_funds` 指定链式顺序。
- **主流程**：`get_filter_chain()` 解析环境变量构建链，`apply_filter_chain(candidates, filters)` 依次应用。

## 实现要点
- 新增 `src/backtest/filters/`：base、filtered_candidates_csv、max_funds、registry。
- `load_fund_nav_data` 增加 `allowed_codes` 参数。
- `get_available_symbols(nav_dir)` 用于获取初始候选集。
- `pybroker_fund_backtest.py` 集成过滤器链，无新增 CLI 参数。

## 验收命令
```bash
cd /Users/zhuaoyuan/cursor-workspace/finance
source myanalyser/.venv312/bin/activate
pytest myanalyser/tests/test_backtest_pybroker_comprehensive.py -v

# 带过滤器运行（需有对应 run 产物）
export FUND_BACKTEST_FILTERS=filtered_candidates,max_funds
export FILTERED_FUND_CANDIDATES_CSV=finance-runs/run_xxx/artifacts/full_run_xxx/filtered_fund_candidates.csv
export FUND_BACKTEST_MAX_FUNDS=50
python myanalyser/tools/pybroker_fund_backtest.py --nav-dir finance-runs/run_xxx/data --strategy low_risk_debt ...
```

## 变更文件
- `myanalyser/src/backtest/filters/`（新增）
- `myanalyser/src/backtest/data.py`（get_available_symbols、allowed_codes）
- `myanalyser/src/backtest/__init__.py`
- `myanalyser/tools/pybroker_fund_backtest.py`
- `myanalyser/tests/test_backtest_pybroker_comprehensive.py`
- `myanalyser/docs/README.md`

## Code Review 后续调整（2026-03-11）
- **data.py**：`allowed_codes` 与文件 stem 比较时对 stem 做相同归一化（`str(p.stem).strip().zfill(6)`），避免 1.csv 与 allowed={"000001"} 误判排除；docstring 统一中文（provided→传入）。
- **filtered_candidates_csv.py**：`是否过滤 == '否'` 前对值做 `astype(str).str.strip()`，兼容前后空格。
- **registry.py**：`apply_filter_chain` 的 docstring 补充“若某步结果为空则提前终止”。
- **测试**：新增 `test_load_fund_nav_data_allowed_codes_normalizes_stem`，覆盖 stem 非 6 位（如 1.csv）时的归一化逻辑。
- **Security**：环境变量指向任意路径的顾虑，CLI 单用户场景可接受，暂不添加路径校验。
