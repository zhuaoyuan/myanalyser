# 基金申购/赎回费率抓取脚本 - 测试总结

日期：2026-03-13

## 1. 场景清单

### 1.1 正常场景

| ID | 场景描述 | 测试用例 |
|----|----------|----------|
| N01 | 金额阶梯解析（小于/大于等于/区间） | `test_parse_amount_tier` |
| N02 | 期限阶梯解析（天/年/混合） | `test_parse_period_tier`, `test_parse_period_tier_days_and_years_mixed` |
| N03 | 费率百分比与固定金额解析 | `test_parse_fee_value` |
| N04 | 多列费率取最小值（优惠费率） | `test_choose_min_fee` |
| N05 | 基金记录加载、去重 | `test_load_fund_records` |
| N06 | 开放申购+开放赎回时正常查询并写入 | `test_run_with_mocked_akshare` |
| N07 | 缺失申购/赎回状态列时全部查询（兼容旧版） | `test_run_no_status_columns_queries_all` |

### 1.2 异常场景

| ID | 场景描述 | 测试用例 |
|----|----------|----------|
| E01 | 缺少基金代码列抛出 ValueError | `test_load_fund_records_missing_code_column` |
| E02 | API 返回 None/空数据时写入异常日志 | `test_run_no_fee_data_writes_exception_log` |
| E03 | 某基金 API 异常时继续处理其他基金 | `test_run_api_exception_continues` |
| E04 | 申购/赎回非开放时不查 API，输出空费率行 | `test_run_skip_fee_query_when_not_open` |
| E05 | 输入文件不存在时 main 返回 1 | `test_main_input_file_not_found` |

### 1.3 边界条件

| ID | 场景描述 | 测试用例 |
|----|----------|----------|
| B01 | 空值、---、em dash、pd.NA 解析为 (None, None) | `test_parse_amount_tier_boundary_empty_and_em_dash` |
| B02 | 基金代码补齐 6 位、去重 | `test_load_fund_records_code_normalization`, `test_normalize_code` |
| B03 | 限大额、开放 等非开放状态不查询 | `test_run_limit_big_amount_skip_query` |
| B04 | 状态判断精确匹配（开放申购/开放赎回） | `test_is_open_for_trade` |

### 1.4 未覆盖（脚本单线程，无并发）

| ID | 场景描述 | 说明 |
|----|----------|------|
| - | 并发冲突 | 脚本为串行处理，无并发场景 |

---

## 2. 测试执行结果

```bash
cd /Users/zhuaoyuan/cursor-workspace/finance
source myanalyser/.venv312/bin/activate
pytest myanalyser/tests/test_fetch_fund_fee.py -v
```

### Summary

| 指标 | 数值 |
|------|------|
| Total Tests | 20 |
| Passed | 20 |
| Failed | 0 |
| Coverage | N/A（需单独配置 cov 源路径） |

### Failures Detail

无失败用例。

| Test ID | Scenario | Input | Expected | Actual | Traceback/Error |
|---------|----------|-------|----------|--------|-----------------|
| - | - | - | - | - | 全部通过 |

---

## 3. 结论

- **代码实现**：当前实现与需求一致，解析逻辑、状态过滤、异常处理均符合预期。
- **需求理解**：无歧义，测试用例覆盖需求文档中的验收点和边界条件。
- **建议**：后续若需覆盖率报告，可在 `pytest.ini` 或 `pyproject.toml` 中配置 `--cov=myanalyser/tools --cov-report=term-missing`。
