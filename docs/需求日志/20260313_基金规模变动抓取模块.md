# 需求日志：基金规模变动抓取模块

日期：2026-03-13

## 背景与目标

- 封装基金规模变动数据抓取为正式模块，akshare 风格 API。
- 输入：基金编码；输出：包含 日期、期间申购（亿份）、期间赎回（亿份）、期末总份额（亿份）、期末净资产（亿元）、净资产变动率 的 pandas DataFrame。
- 数据来源：东方财富 FundArchivesDatas API，解析 `content` 内 HTML 表格。

## 设计与风险

### 设计要点

- **API 选择**：页面 `gmbd_{code}.html` 为 JS 动态渲染，直接请求仅返回 loading 占位。采用 FundArchivesDatas API 获取结构化数据，解析 `content` 内 HTML 表格。
- **接口风格**：`fund_gmbd_em(code: str) -> pd.DataFrame`，与 akshare 命名与返回值风格一致。
- **CLI**：`tools/fetch_fund_gmbd.py` 支持单/多基金编码、CSV 输入、输出 CSV，带 `--delay` 请求间隔。

### 验收清单

```bash
cd /Users/zhuaoyuan/cursor-workspace/finance
source myanalyser/.venv312/bin/activate

# 单测
python -m unittest discover -s myanalyser/tests -p "test_fund_gmbd.py" -v

# 模块用法
python -c "from fund_gmbd import fund_gmbd_em; import sys; sys.path.insert(0, 'myanalyser/src'); from fund_gmbd import fund_gmbd_em; print(fund_gmbd_em('000198').head())"

# CLI
python myanalyser/tools/fetch_fund_gmbd.py 000198 -o /tmp/gmbd.csv
python myanalyser/tools/fetch_fund_gmbd.py -i fund_purchase.csv -o gmbd_out.csv --delay 0.3
```

## 实现摘要

### 变更文件

- `myanalyser/src/fund_gmbd.py`（新增）：核心模块，`fund_gmbd_em()`、`fetch_gmbd_api()`、`_parse_gmbd_from_content()`
- `myanalyser/tests/test_fund_gmbd.py`（新增）：解析逻辑、mock API、本地 HTML 样例
- `myanalyser/tools/fetch_fund_gmbd.py`（新增）：CLI，支持编码列表、CSV 输入、输出 CSV
- `myanalyser/tools/test_fund_gmbd.py`（删除）：可行性测试脚本，已由正式模块替代
- `myanalyser/docs/README.md`（更新）：核心脚本列表
- `myanalyser/docs/系统设计.md`（更新）：核心模块职责

### 关键输出样例

```
日期        期间申购（亿份）  期间赎回（亿份）  期末总份额（亿份）  期末净资产（亿元）  净资产变动率
2025-12-31  12392.31        12660.77        7646.28          7646.28         -3.39%
2025-09-30  12812.89        12830.34        7914.74          7914.74         -0.22%
...
```

## 测试场景清单（2026-03-13 补充）

### 正常场景
| ID | 场景 | 函数/模块 |
|----|------|----------|
| N1 | 短编码补零（198→000198） | _normalize_code |
| N2 | 六位完整编码 | _normalize_code |
| N3 | 去除首尾空格 | _normalize_code |
| N4 | 千分位数值解析（12,392.31） | _parse_num |
| N5 | 元转亿元（1239230971212.3→12392.31） | _to_yiyuan |
| N6 | 净资产变动率格式化（-3.39→-3.39%） | _to_pct |
| N7 | 空 HTML / 无表格返回空 DataFrame | _parse_gmbd_from_content |
| N8 | 有效表格解析（含千分位、---） | _parse_gmbd_from_content |
| N9 | 无 gmbd class 的 table 回退解析 | _parse_gmbd_from_content |
| N10 | API data 分支返回完整 DataFrame | fund_gmbd_em |
| N11 | API content 分支（data 为空时解析 HTML） | fund_gmbd_em |
| N12 | CSV 加载基金代码、去重 | _load_codes_from_csv |

### 异常场景
| ID | 场景 | 函数/模块 |
|----|------|----------|
| E1 | ---、空字符串、None 解析为 None | _parse_num |
| E2 | 非法字符串（abc、N/A）解析为 None | _parse_num |
| E3 | _to_yiyuan 空值/--- 返回 None | _to_yiyuan |
| E4 | _to_pct 空值返回 None | _to_pct |
| E5 | API 返回空 content+data，返回空表 | fund_gmbd_em |
| E6 | 网络异常向上抛出 | fund_gmbd_em |
| E7 | 空/非数字编码 fetch_gmbd_api 直接返回空 | fetch_gmbd_api |
| E8 | CSV 缺少“基金代码”列抛出 ValueError | _load_codes_from_csv |

### 边界条件
| ID | 场景 | 函数/模块 |
|----|------|----------|
| B1 | 空字符串编码 → "" | _normalize_code |
| B2 | 非纯数字保留原样（abc） | _normalize_code |
| B3 | int 类型编码（198）转为 000198 | _normalize_code |
| B4 | 零值、负数、大数解析 | _parse_num |
| B5 | 极小值转亿元（1000000→0.01） | _to_yiyuan |
| B6 | 字符串形式数字（API 可能返回） | _to_yiyuan |
| B7 | 无效编码 fund_gmbd_em 返回空表且不请求 API | fund_gmbd_em |
| B8 | 货币基金 QMJZC 为空时用 QMZFE 填充 | fund_gmbd_em |
| B9 | ZFEBDL 缺失时使用 CHANGE 字段 | fund_gmbd_em |
| B10 | 行内部分字段为 null | fund_gmbd_em |
| B11 | td 数量与 th 不一致的行被跳过 | _parse_gmbd_from_content |

### 未覆盖（可选后续）
- **并发**：多线程/多进程同时请求同一基金（当前单测 mock 网络，无实际并发）
- **极大值**：超大数据（如 1e20 元）的精度与展示
```
