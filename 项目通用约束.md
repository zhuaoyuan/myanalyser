*（强制）执行项目中的 python 脚本需要切换到虚拟环境：
```
cd /Users/zhuaoyuan/cursor-workspace/finance
source myanalyser/.venv312/bin/activate
```
* 每次改动代码都需要设计测试用例，每一步任务可以自动化闭环验证。
* 本项目的开发者-ai协作方式遵循固定工作流：需求澄清（开发者） -> 设计与风险（双方对话） -> 实现（ai） -> 验收证据（ai）。【设计与风险】阶段，ai需充分与开发者讨论 设计/选型/功能细节等方面疑议和选项，最终需在开始实现前先给“验收清单（可执行命令）”。
* 【实现】阶段，交付必须包含：变更文件列表、新增/修改测试点、本地执行命令、关键输出样例。
* 【验收证据】阶段，至少一个统一命令（例如 make verify）必须跑完：单测 + 核心 CLI smoke + 数据完整性检查。（TODO）
